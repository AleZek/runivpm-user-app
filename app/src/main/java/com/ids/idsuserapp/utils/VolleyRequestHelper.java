package com.ids.idsuserapp.utils;

import com.ids.idsuserapp.db.entity.AppEntity;
import com.ids.idsuserapp.viewmodel.AppViewModel;

import org.json.JSONArray;

/**
 * Created by zek on 12/04/18.
 */

public class VolleyRequestHelper<T extends AppViewModel, E extends AppEntity> {
        private void persistCollection(JSONArray mappe, T viewModel) {
        }

    //TODO trovare una soluzione per gestire il response delle richieste con i generics

}
